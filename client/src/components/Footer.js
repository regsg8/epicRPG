import React from 'react';

const Footer = () => {
    const styles = {
        footer: {
            padding: '10px',
            border: '2px blue solid',
            height: '18px'
        }
    };

    return (
        <div style={styles.footer}>
            
        </div>
    )
}

export default Footer