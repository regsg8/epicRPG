import React from 'react';

const Footer = () => {
    const styles = {
        footer: {
            position: 'fixed',
            bottom: '0',
            width: '100%',
            padding: '10px',
            border: '2px blue solid',
            height: '18px'
        }
    };

    return (
        <div style={styles.footer}>
            
        </div>
    )
}

export default Footer